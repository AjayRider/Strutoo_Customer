<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="model"
            type="com.strutoocustomer.customer.recycleradapter.DummyModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{(v)->model.onItemClick.onClick(v, model.adapterPosition, `all`)}"
        android:id="@+id/childLayoutCategory">

        <TextView
            android:id="@+id/tvfirstitem"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/_111111"
            android:text="Face"
            app:layout_constraintBottom_toBottomOf="parent"
            android:textSize="@dimen/_11ssp"
            android:textStyle="normal"
            android:textAllCaps="true"
            android:fontFamily="@font/bw_modelica_medium"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <ImageView
            android:id="@+id/ivarrow"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_forward"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <View
            android:id="@+id/viewfirstitem"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="#e6e6e6"
            android:layout_marginTop="@dimen/_10sdp"
            app:layout_constraintTop_toBottomOf="@id/tvfirstitem"
            app:layout_constraintStart_toStartOf="@id/tvfirstitem"
            app:layout_constraintEnd_toEndOf="@id/ivarrow"/>



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>